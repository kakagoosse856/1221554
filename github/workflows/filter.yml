name: Filter Channels

on:
  workflow_dispatch:
  schedule:
    - cron: "*/30 * * * *"  # كل 30 دقيقة (اختياري)

permissions:
  contents: write  # مهم جداً

jobs:
  filter:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true  # 🔥 هذا يسمح بالpush بدون 403

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install requests

      - name: Run filter script
        run: python filter.py

      - name: Commit & push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # إنشاء فرع تلقائي أو استخدم main لو تحب
          git checkout -B auto-update

          git add *.m3u
          git diff --cached --quiet || git commit -m "Update alive channels"
          git push origin auto-update --force
