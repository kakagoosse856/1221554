name: bein

on:
  schedule:
    - cron: '0 * * * *'   # كل ساعة UTC
  workflow_dispatch:

jobs:
  update-playlist:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install Requests
        run: pip install requests

      - name: Run IPTV Update Script
        env:
  GITHUB_USER: "kakagoosse856"
  GITHUB_REPO: "1221554"
  GITHUB_TOKEN: "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"
        run: python3 bein.py
