name: Auto Update ✪ eggggptt M3U

on:
  schedule:
    - cron: '0 * * * *'  # كل ساعة
  workflow_dispatch:    # يمكن تشغيله يدويًا

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests beautifulsoup4

      - name: Generate M3U
        run: python eggggptt.py

      - name: Commit & Push changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          if [ -f eggggptt.m3u ]; then
            git add eggggptt.m3u
            git commit -m "Auto update eggggptt.m3u" || echo "No changes to commit"
            git pull --rebase
            git push
          else
            echo "eggggptt.m3u does not exist, skipping commit."
          fi
